<!-- res/drawable/spinner_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Màu nền -->
    <stroke
        android:width="1dp"
        android:color="@color/green" /> <!-- Đường viền -->
    <corners android:radius="8dp" /> <!-- Bo góc -->
    <padding
        android:left="2dp"
        android:top="2dp"
        android:right="4dp"
        android:bottom="2dp" /> <!-- Khoảng cách nội dung bên trong -->
</shape>
